name: Publish Docker image

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:main
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}-${{ github.sha }}
          build-args: |
            DATA_GO_KR_SERVICE_KEY=${{ secrets.DATA_GO_KR_SERVICE_KEY }}
            DATA_GO_KR_SERVICE_KEY_ENCODED=${{ secrets.DATA_GO_KR_SERVICE_KEY_ENCODED }}
            KAKAO_REST_API_KEY=${{ secrets.KAKAO_REST_API_KEY }}
            ODSAY_API_KEY=${{ secrets.ODSAY_API_KEY }}
            KFTC_FINMAP_CLIENT_ID=${{ secrets.KFTC_FINMAP_CLIENT_ID }}
            KFTC_FINMAP_CLIENT_SECRET=${{ secrets.KFTC_FINMAP_CLIENT_SECRET }}
            KFTC_FINMAP_BASE_URL=${{ secrets.KFTC_FINMAP_BASE_URL }}
